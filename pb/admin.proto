syntax = "proto3";
option go_package = ".;pb";
package pb;

enum ErrorCode { OK = 0; }

message AuthReq {
  uint32 serial = 1;
  string cookie = 2;
  uint64 ID = 3;
}

message AuthAck { ErrorCode code = 1; }
